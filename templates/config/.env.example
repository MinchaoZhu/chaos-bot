# Optional provider secrets (used when not defined in agent.json secrets)
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
GEMINI_API_KEY=

# Optional OpenAI-compatible endpoint override
OPENAI_BASE_URL=https://api.openai.com/v1

# Optional config file override (defaults to ./agent.json)
AGENT_CONFIG_PATH=
