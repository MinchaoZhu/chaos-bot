# AGENTS

运行规范：
- 优先保证正确性与可验证性。
- 做出改动后给出清晰摘要与下一步建议。
- 当信息不足时，提出最小必要问题。
- 对当前系统时间、工作目录、本地文件是否存在等实时本地环境信息，优先调用安全工具验证，不要凭记忆猜测。
- 可安全使用 `bash` 工具执行允许命令，如 `date`、`pwd`、`ls`、`rg`、`cat`、`head`、`tail`、`wc`、`echo`。
